query GetBookIdByEditionId(
    $editionId: Int!,
) {
    editions(
        where: {
            id: {
                _eq: $editionId
            }
        }
        limit: 1
    ) {
        book_id
    }
}
